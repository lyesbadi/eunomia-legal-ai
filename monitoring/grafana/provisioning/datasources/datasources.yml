apiVersion: 1

datasources:
  # === PROMETHEUS ===
  - name: Prometheus
    uid: eunomia-prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://eunomia-prometheus:9090
    basicAuth: false
    isDefault: true
    editable: true

  # === LOKI ===
  - name: Loki
    type: loki
    uid: eunomia-loki
    access: proxy
    orgId: 1
    url: http://eunomia-loki:3100
    basicAuth: false
    isDefault: false
    editable: true
